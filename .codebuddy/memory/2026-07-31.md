# 2026-07-31

## lottery 页面新增 app 环境独立组件
- 创建 `src/assets/css/vip/lottery/index-app.less`，用于 app 环境（`__evn=app`）定制样式
- 创建 `src/views/vip/lottery/index-app.vue`，app 环境独立 Vue 组件（含独立 less 引入）
- 修改 `src/views/vip/lottery/index.vue`：
  - 引入 `IndexApp` 组件并注册
  - `v-if="isApp"` 时渲染 `<IndexApp />`，否则渲染原有内容
  - `isApp` 计算属性通过 `this.$route.query.__evn === 'app'` 判断

## lottery app 组件按设计稿重构为刮卡流程
- 重写 `src/views/vip/lottery/index-app.vue`：
  - 签到状态、当前可刮奖次数、积分换次数入口
  - 本期奖池缩略与详情展开
  - 2x2 刮卡网格，支持单点刮卡（Canvas 刮开效果）
  - 连刮九次 / 全部刮完 / 我的奖品底部操作
  - 单点、连刮、结果展示弹层
  - 积分兑换 Action Sheet
  - 我的奖品独立页面
- 重写 `src/assets/css/vip/lottery/index-app.less`：移动端紫色主题刮卡页面样式

## lottery app 组件接入线上图片资源
- 替换 `index-app.vue` 占位资源为 `https://cdn.jx3box.com/design/event/lottery/app/` 下的真实图片：
  - 背景：`bg.jpg`
  - 标题：`title.png`
  - 刮奖次数图标：`cishu.jpg`
  - 积分图标：`jifen.jpg`
  - 谢谢惠顾：`miss.jpg`
  - 刮卡背面：`card-0.jpg`（网格卡）、`card-1.jpg`（单点刮层）、`card-mini.jpg`（连刮小卡）
- 更新 `index-app.less`：头部改用真实背景图，刮卡网格使用图片背景，同步调整相关样式

## lottery app 组件按设计图细节调整
- `index-app.vue`：
  - 移除未使用的 `starIcon` 方法
  - 所有星级统一使用 `star1.svg`
  - 规则说明移除左侧 `el-icon-info` 图标
- `index-app.less`：
  - 标题图片宽度由 200px 调整为 160px
  - 状态卡 padding 与图标尺寸微调
  - 奖池缩略图/详情图/我的奖品记录图统一调整为 64×64px，圆角 12px
  - 规则栏样式保持与设计图一致

## lottery app 组件按实际显示截图修复
- `index-app.vue`：
  - 修复 `setPrizeList` 星级计算：`Math.max(1, 5 - index)`，使奖池按顺序显示 5/4/3/2/1 星
- `index-app.less`：
  - 状态卡 `.m-actions` 明确 `flex-direction: column`、`align-items: stretch`，按钮 `display: block; width: 100%`，确保签到与积分换次数两个按钮垂直堆叠可见
  - 标题图片宽度 `160px → 180px`
  - 规则栏「规则说明」「换一批」颜色加深为 `#6a5af9` 并加粗
  - 奖池缩略奖品名称限制最多 2 行，固定最小高度避免星级位置跳动

## lottery app 组件二次按设计图全面调整
- `index-app.less`：
  - 头部 `.m-header`：`min-height 260px`、`background-size: cover`、返回按钮半透明毛玻璃 `36px`
  - 标题图片 `200px`，`margin-top: 60px`，副标题色 `#9c8ed4`
  - 状态卡 `margin-top: -50px`、按钮区 `flex-shrink: 0`、单个按钮固定 `width: 100px`、`text-align: center`
  - 奖池缩略图调整为 `60×60px`，背景渐变+阴影，详情图调整为 `48×48px`
  - 奖池「奖池详情」颜色 `#6a5af9`
  - 底部操作栏按钮均为紫色渐变，`flex: 1.35` + `flex: 1` 比例，「我的奖品」橙色渐变
  - 我的奖品页 `.m-header` 独立缩至 `160px` 并重置 `margin-top: 40px`

## lottery app 组件按设计图最终精调
- `index-app.vue`：
  - 新增 `starIcon(index)`：首位大奖使用 `star1.svg`，其余使用 `star2.svg`，对应设计图橙色/紫色星级区分
  - 奖池缩略与详情星级均改用 `starIcon(index)`
- `index-app.less`：
  - 规则栏增加半透明白色圆角背景、投影，三项文字更鲜明
  - 奖池列表改为 `justify-content: space-evenly`，视觉等间距
- 修复状态卡层级：`.m-status-card` 增加 `position: relative; z-index: 10`（原先只有 z-index 无 position，导致 z-index 不生效，卡片被 header 背景盖住）
