# 2026-08-10

## lottery app 组件奖池按设计图精细调整
- `src/views/vip/lottery/index-app.vue`：
  - 奖池缩略项增加 `:class="{ 'is-top': index === 0 }"`，给首位大奖（神秘大奖）添加橙色描边
- `src/assets/css/vip/lottery/index-app.less`：
  - 奖池 `.m-prize-list` 改为 `justify-content: space-between`，`.m-prize-item` 改为 `flex: 1` + `max-width: 90px`，平均分布
  - 奖品缩略图 `60×60px → 70×70px`，圆角 `14px → 16px`，加 `2px solid #ede6ff` 描边
  - `.is-top .u-img-wrap` 描边色 `#ffb84d`（橙色，对应神秘大奖）
  - 奖品名称由 2 行截断改为 1 行省略（设计图奖品名都只有一行），字号 `11px → 13px`
  - 星级 `11px → 14px`，容器 `min-height: 16px` 居中，使用 `gap: 3px` 替代 `margin-right`

## 修正边框位置（用户反馈）
- 设计稿边框应在 `m-prize-item` 而非 `u-img-wrap`
- `.m-prize-item`：增加 `2px solid #ede6ff` 边框 + `16px` 圆角 + `padding: 6px 4px 10px`（边框包裹整块奖品项，含图片、名称、星级）
- `.u-img-wrap`：移除边框，缩回 `64×64px`、圆角 `12px`
- `.is-top`（神秘大奖）改为直接作用在 `.m-prize-item` 上：`border-color: #ffb84d`

## 图片填充整卡调整（用户反馈）
- `.u-img-wrap` 改为填充整个 `.m-prize-item`：`width: 100%` + `aspect-ratio: 1/1`，不再是小缩略图居中
- `.m-prize-item` padding 收敛为 `5px`（底部 `10px`）
- `.is-top` 的 `.u-img-wrap` 背景改暖橙渐变 `#fff4e0 → #ffe9c9`

## 星级颜色修正（用户反馈）
- `starIcon(index)`：`.is-top`（index 0）使用 `star2.svg`（黄色），其余使用 `star1.svg`（紫色）——此前两者互换导致颜色反了

## 奖池缩略改为走马灯（用户需求）
- 不再 `slice(0, 4)`，改为渲染全部 `previewList`；走马灯需内容重复两份实现无缝循环：
  - 模板：`.m-prize-list > .m-prize-track` 内渲染两遍 `<template v-for>`（key 放 template 上：`'a-'+index` / `'b-'+index`），第二份加 `aria-hidden`
- CSS：
  - `.m-prize-list`：`overflow: hidden`，去掉 flex 布局
  - `.m-prize-track`：`display: flex; gap: 10px; width: max-content; animation: prize-scroll 30s linear infinite;` hover 暂停
  - `.m-prize-item`：`flex: 0 0 100px; width: 100px`（固定宽度保证 track 正常滚动）
  - `@keyframes prize-scroll`：`translateX(0)` → `translateX(-50%)`，两份内容位移 50% 即无缝回环

## 走马灯点击查看奖品详情（用户需求）
- 点击走马灯任一奖品：弹窗展示图片+名称+中奖率+介绍，滚动暂停，被点击项加 `is-top` 高亮；关闭弹窗恢复滚动、移除高亮
- 模板：
  - `.m-prize-track` 加 `:class="{ 'is-paused': !!activePrize }"`
  - 走马灯 item 加 `@click="selectPrize(item, index)"`，`is-top` 改为 `isTopClass(index)`
  - 新增奖品详情弹窗 `.m-prize-dialog`（图片 120px、名称、中奖率、介绍、知道了按钮）
- 数据：`activePrize`、`selectedIndex`
- 方法：`isTopClass(index)`（未选中默认首位大奖，选中后高亮选中项）、`selectPrize`、`closePrizeDetail`（重置 `activePrize`/`selectedIndex` 为 null）
- CSS：`.m-prize-track.is-paused` 暂停动画；新增 `.m-prize-dialog` 弹窗样式
- ⚠️ 修复 v-if/v-else 配对 bug：插入弹窗 `v-if="activePrize"` 后，原 `m-prize-detail` 的 `v-else` 被 Vue 误配到弹窗的 v-if，导致 activePrize 为 null 时走马灯和详情列表同时显示。改为显式 `v-if="showDetail"`。**Vue 的 v-else 必须紧跟 v-if，中间插入任何元素都会破坏配对。**
- `isTopClass(index)` 改为：默认所有 item 无 `is-top`，仅点击选中项高亮（`selectedIndex !== null && index === selectedIndex`）——用户明确要求默认不加 is-top

## 奖池详情改为弹窗显示 odds（用户需求）
- 「奖池详情」不再是展开列表，点击后弹出 `.m-odds-dialog` 用 `v-html` 渲染 `this.odds`
- 模板：`.u-more` 点击改为 `showOdds = true`（文案固定「奖池详情」，图标改右箭头）；删除 `m-prize-detail` 展开列表块；走马灯 `m-prize-list` 去掉 `v-if="!showDetail"` 始终显示
- data：`showDetail` → `showOdds`
- CSS：新增 `.m-odds-dialog`（与规则弹窗同风格，含 img/table 适配）
- 用户反馈 odds 弹窗显示纯文字无 HTML：`v-html="odds"` 用法正确，`getBreadcrumb` 返回 `data.html`；若渲染为纯文字，说明 CMS 中 `lottery_odds_{ID}` 编辑内容就是纯文本。已在 `.u-odds-content` 加 `white-space: pre-wrap` + `word-break: break-word` 增强纯文本可读性
- `.u-rules-content` 也加了与 `.u-odds-content` 相同的 `white-space: pre-wrap` + `word-break: break-word`
- `buildCards()` 默认刮卡数量由 4 张改为 10 张（`new Array(10)`），网格 2 列 × 5 行
- `handleSign()` 增加未登录判定：`if (!this.isLogin) return this.toLogin();`（与其他需登录操作一致）
- `u-refresh`（换一批）点击后刷新下方卡片组并显示 loading 动画：
  - `refreshCards()`：加 `isRefreshing` 状态，800ms 后恢复；防止连点
  - 模板：`.m-card-grid` 绑定 `:class="{ 'is-refreshing': isRefreshing }"`，内加 `.m-refresh-mask`（`el-icon-loading` 旋转图标 + 半透明遮罩）
  - CSS：`@keyframes card-refresh-in`（卡片从 `scale(0.88)+translateY(8px)` 淡入），`.is-refreshing .m-scratch-card` 播放该动画
- 奖池详情改回展开列表：撤销 odds 弹窗方案。`showOdds` → `showDetail`；「奖池详情/收起」切换 `m-prize-track`（`v-if="!showDetail"`）与 `m-prize-detail` 列表（`v-if="showDetail"`）；删除 `.m-odds-dialog` 样式与模板
- 登录后奖品获取两种方式精调：
  - 单卡弹窗 `.m-scratch-stage`：`background: linear-gradient(135deg, #b7a9f7, #8473ed)` 紫色渐变（去白底），尺寸 `280×340px`，阴影紫色调；奖品用 `.u-prize-card` 白色圆角容器包裹（紫卡中露出白色奖品区，符合设计图2「云朵」效果）
  - 九张连刮 `.m-batch-dialog`：3×3 + card-mini.jpg + 「点击任意位置刮开」+「拿下！」已实现，紫色渐变与设计图一致
- 修复 `m-scratch-card` 点击无反应：原 `if (this.remainingCount < 1 || this.isDrawing) return;` 静默返回无提示。改为：
  - 次数不足时 `$message.warning("刮奖次数不足，可签到或使用积分兑换次数")`
  - `isDrawing` 异常卡死时自动重置：`if (this.isDrawing && !this.showSingleScratch && !this.showBatchScratch) this.isDrawing = false;`
  - `openBatchScratch` 同样处理
- 单卡刮层调整：`initScratchCanvas` 覆盖图由 `card-1.jpg` 改为 `card-mini.jpg`；canvas 增加 `is-done` 类，刮开后 `display: none` 彻底隐藏覆盖层，只露出下方中奖图片（u-prize-card 白色圆角容器）
- `.u-prize-card` 改为铺满整个 `.m-scratch-stage`（`width/height: 100%`，reveal padding 归零），奖品图片 `150×150px` 居中
- 刮卡体验优化：
  - `scratch(ctx, x0, y0, x1, y1)` 从「每帧画圆点」改为「moveTo/lineTo 连线 + stroke」：`lineCap/lineJoin: round`、`lineWidth: 48`（更粗）、圆头连接连续无断点（更顺滑）
  - `onScratchStart` 记录 `lastScratchPos`，`onScratchMove` 连线到新点后更新，`onScratchEnd` 清空
- `claimSingle`（单卡「拿下！」）：去掉 `resultPrizes/showResult`，不再叠加显示结果 m-overlay，直接关闭单卡弹窗
- 单卡刮开后将原卡片转换为奖品显示（不再刷新全部）：
  - 新增 `activeCardIndex` 记录被点击的卡片索引
  - `openSingleScratch(card, index)`：传参记录索引，已 scratched 卡片不响应
  - goodLucky/fetchPrize 成功后将该卡 `scratched = true` 且 `prize = currentPrize`
  - `claimSingle` 不再 `refreshCards()`，仅重置 `activeCardIndex`
  - `.u-reveal` 调优：图片 `65%/60%`、border-radius 8px、z-index 1，避免卡背景透出
- 修复单卡弹窗 card-mini.jpg 不第一时间显示（图片异步加载前 canvas 空白透出奖品）：
  - data 加 `scratchReady`，canvas 用 `:style` 在 `!scratchReady` 时以 CSS `backgroundImage` 立即铺 card-mini.jpg（`background-size: cover`）
  - `initScratchCanvas` 图片 onload/onerror 后 `scratchReady = true` 移除 CSS 背景（此时 canvas 已绘制内容）
  - `openSingleScratch` 打开时重置 `scratchReady = false`，保证每次打开都有占位背景
- 单卡刮层再优化（去掉 CSS cover 背景方案，改为立即可刮）：
  - 移除 canvas 的 CSS `backgroundImage` 占位与 `scratchReady` 状态、`background-size: cover` 样式（cover 拉伸比例与 drawImage 不一致导致图片「动」/变形）
  - `initScratchCanvas` 打开瞬间立即 `fillStyle="#9c8af0"` 纯色填充 + `destination-out`，`scratchCtx` 立即就绪 → 第一时间即可刮卡
  - 图片加载成功后切 `source-over` → `clearRect` → `drawImage` 重绘 card-mini.jpg → 恢复 `destination-out`；加载失败保持纯色刮层
- 单卡刮层最终方案（card-mini.jpg 直接显示 + canvas 异步加载）：
  - stage 内新增 `.u-cover` div（z-index 3，`background-size: 100% 100%` 拉伸铺满 card-mini.jpg），加载期间直接显示图片，与 drawImage 拉伸一致无跳动
  - canvas（z-index 2）加载前保持透明；`initScratchCanvas` 图片 `onload` 后 `drawImage` + `destination-out` 并 `scratchReady = true`（隐藏 u-cover），`onerror` 填充纯色刮层
  - data `scratchReady` 重新引入，`openSingleScratch` 打开时重置
- 弹窗打开时锁定背景滚动：computed `hasOverlay`（showSingleScratch/showBatchScratch/showResult/showExchange/showRules/activePrize 任一为真），watch 中 `document.body.style.overflow = val ? "hidden" : ""`，关闭弹窗自动恢复
- 修复刮卡起始位置不跟随手指：
  - `.u-cover`（z-index 3，位于 canvas z2 之上）加 `pointer-events: none`，触摸事件穿透到 canvas
  - `initScratchCanvas`：打开瞬间立即纯色填充 + destination-out，`scratchCtx` 立即可用（起始位置即手指按下处）
  - 图片 onload 后保留刮痕合成：先复制当前 canvas（纯色+刮痕 alpha）到临时 canvas 作为 mask → 清空 → source-over 画 card-mini.jpg → destination-in 用 mask 裁出（未刮区保留图片，已刮区透明露出奖品）→ 恢复 dpr transform 与 destination-out；注意 mask 操作用物理像素坐标（`setTransform(1,0,0,1,0,0)`）
- ⚠️ 修复刮卡异常（奖品直接露出 + 图片缩在左上角）：onload 中切换物理坐标（`setTransform(1,0,0,1,0,0)`）后 `drawImage(img, 0, 0, rect.width, rect.height)` 误用逻辑尺寸，图片只画左上 1/4（dpr=2 时），其余透明导致奖品透出。改为 `drawImage(img, 0, 0, canvas.width, canvas.height)`（物理坐标必须用物理尺寸）
- 刮卡起始位置进一步加固：
  - 刮卡触摸/鼠标事件从 canvas 移到 `.m-scratch-stage`（`ref="scratchStage"`），避免 u-cover 等子层拦截事件导致起点错位；`getScratchPos` 优先用 stage rect
  - `onScratchStart` 无条件记录 `lastScratchPos` + `isScratching = true`（不再因 `scratchCtx` 缺失提前 return 丢起点），`scratchCtx` 就绪后从起点绘制
  - `initScratchCanvas` 对 `getBoundingClientRect` 为 0 的情况兜底（280×340）
- ⚠️ 彻底重构刮卡坐标系：统一**物理像素坐标**，去掉 `ctx.scale(dpr, dpr)`（dpr 缩放与 onload 的 setTransform 切换易致坐标错乱，点击中心却擦除在左上方）：
  - `initScratchCanvas`：去掉 `ctx.scale`，填充/绘制全部用 `canvas.width/height`（物理尺寸）；onload 合成不再 setTransform
  - `getScratchPos`：返回 `(clientX - rect.left) * dpr`（物理像素）
  - `scratch`：`lineWidth = 48 * dpr`（保持逻辑 48px 粗度）
  - `checkScratchProgress`：`clearRect(0, 0, canvas.width, canvas.height)` 全图清除
- 刮卡起点偏移最终修复（用户反馈：仅起点不跟随，后续轨迹跟随）：
  - 根因：触摸后浏览器补发**模拟 mousedown**，第二个 `onScratchStart` 用模拟事件的坐标（可能 offsetX=0/错误）把起点重画到错误位置（左上角/中央）
  - `getScratchPos` 去掉 `offsetX` 分支，统一 `clientX/clientY`（触摸 `touches[0]`，鼠标 `e.clientX`）——两事件坐标系一致
  - `onScratchStart` 加 `touchScratching` 标志：触摸中忽略模拟 `mousedown`（`e.type === "mousedown" && this.touchScratching` 则 return）；`onScratchEnd` 中 `touchend` 时复位标志
  - data 新增 `touchScratching: false`
- 刮卡定位改用**动态比例**（用户反馈仍偏差，怀疑之前缩放/dpr 问题）：
  - `getScratchPos` 不再用固定 `dpr` 乘算，改为 `scaleX = canvas.width / rect.width`、`scaleY = canvas.height / rect.height`（canvas 物理像素 ÷ CSS 渲染像素）。该比例对页面 zoom / 非整数 dpr / CSS transform 一律精确，从根上消除缩放导致的坐标偏差
  - `scratch` 的 `lineWidth` 同步用该动态比例（`48 * scale`）
  - `initScratchCanvas` 保持 `canvas.width = rect.width * dpr`（物理尺寸），与动态比例自洽
- ⚠️ 定位偏差比例性修复（用户反馈：横偏移 20%、竖偏移 50%）：
  - 根因：canvas/u-cover 用 `width: 100%; height: 100%` 可能导致 CSS 渲染尺寸与 stage（280×340）不一致（被压缩成约 224×170 = 0.8×0.5），`getScratchPos` 用 stage rect 计算相对位置 + 动态比例换算后产生比例性偏差
  - `.u-scratch-canvas` / `.u-cover` 显式固定 `width: 280px; height: 340px; left: 0; top: 0`，与 stage 完全一致
  - `getScratchPos` 优先取 canvas 的 rect（绘制目标基准统一）
  - 注意：stage 的 `position: relative` 必须保留，canvas 才能以 stage 为定位基准
- 我的奖品页面按截图调整：
  - 模板移除 `.m-title` 内 h1「我的奖品」标题，banner 仅保留返回按钮
  - 列表上方加 `.m-section-title-2` 小标题「我的奖品」
  - 奖品项增加 `:class="{ 'is-top': index === 0 }"`，首项（神秘大奖）橙色边框
  - 星级按 index 选择：index 0 用 `star2.svg`（黄色），其余用 `star1.svg`（紫色）
  - `.m-record-item` 增加 `border: 2px solid transparent` 占位（避免有/无边框时高度跳动）；`.is-top { border-color: #ffb84d }`
- 我的奖品页面 banner 修复（截图 vs 设计稿不一致）：
  - 之前 `.m-page .m-header` 只有纯背景小区域，缺少 banner 背景图与标题内容
  - 模板：`.m-page-prizes .m-header` 内补回返回按钮 + `m-title`（`title.png` 标题图片 + 副标题）
  - less：`.m-page .m-header` 复用主页 banner 样式：`background: url(bg.jpg) center top / cover`、`min-height: 260px`、返回按钮半透明毛玻璃、标题图片 `width: 180px; margin-top: 60px`、副标题 `#9c8ed4`
  - 注：用户后续自行把背景从 `.m-header` 移到 `.m-page`（`background: url(bg.jpg) center top no-repeat; background-size: 100% auto; background-color: #ededf9`），并将 `.m-prize-records` 改成白卡边框样式（`margin: 10px; border-radius: 16px; border: 1px solid #e8e8e8`）
- 我的奖品改为分页滚动加载：
  - 模板：`.m-prize-records` 绑定 `@scroll="onRecordsScroll"`，底部加 `u-records-tip`（加载中 / 没有更多了）
  - data 新增：`myPrizePage`、`myPrizeTotal`、`myPrizesLoading`
  - `goMyPrizes`：进入时重置列表/页码/总数，`loadMyPrizes(1)`
  - `loadMyPrizes(page=1)`：`pageSize: 20`；page===1 覆盖列表，否则 concat 合并；记录 `myPrizePage`、`myPrizeTotal`；加 loading 防重
  - `onRecordsScroll`：滚动距底 <60px 且未加载中且 `list.length < total` 时 `loadMyPrizes(page+1)`
  - less：`.m-page` 改 `display: flex; flex-direction: column; overflow: hidden`；`.m-header` `flex-shrink: 0`；`.m-prize-records` `flex: 1; overflow-y: auto; -webkit-overflow-scrolling: touch`；新增 `.u-records-tip`
- 分页加载 bug 修复（接口 total=536 却提示「没有更多了」）：
  - 根因：接口 `total` 字段位置不稳定（不在 `data` 内层），`data.total` 为 undefined 时 `myPrizeTotal` 回落成当前页长度，`length >= total` 提前成立
  - 改为不依赖 total：用「返回条数是否满页（>=20）」判断 `myPrizeHasMore`；「没有更多了」条件改为 `!myPrizeHasMore`；`onRecordsScroll` 用 `myPrizeHasMore` 判断是否继续翻页
  - data 中 `myPrizeTotal` 替换为 `myPrizeHasMore`
- `.m-section-title-2` 吸顶：`position: sticky; top: 0; z-index: 1; background: #fdfdff`（标题不随内容滚动）
- 积分兑换 Action Sheet 改为中间位置弹窗（按设计稿截图）：
  - 模板：移除 overlay 上的 `m-overlay-sheet` 类，让 `.m-overlay` 默认 `align-items: center` 居中
  - less：`.m-action-sheet` 由底部 sheet 样式（`border-radius: 20px 20px 0 0` + 底部安全区 padding）改为中间弹窗样式（`width: 320px; margin: 0 auto; border-radius: 16px; padding: 24px 20px 20px`）
- 🔴 后端接口分页失效（已用 curl 实测确认）：`GET /api/lucky-draw/my/history?page=1&pageSize=20` 与 `page=16&pageSize=20` 返回内容完全相同（字节数均 5180）。后端忽略 `page` 参数，总返回同一批数据（total 536 但分页无效）。前端 `loadMyPrizes` 传参正确，等待后端修复分页。修复前：每页数据重复合并，`myPrizeHasMore` 恒 true。
- `loadMyPrizes` 分页参数名由 `page` 改为 `index`（`getMyHistory({ index, pageSize: 20 })`），并修复遗留 bug：`this.myPrizePage = page`（参数改名后 page 为 undefined）→ `this.myPrizePage = index`
- 我的奖品记录项 `m-record-item` 与主页面一致：默认无 `is-top`，点击后加 `is-top`（再点取消）：
  - data 新增 `activeRecordIndex`；`is-top` 改为 `activeRecordIndex === index`，点击切换（`activeRecordIndex === index ? null : index`）
  - 星级颜色同步：选中项 `star2.svg`（黄色），未选中 `star1.svg`（紫色）
- 我的奖品整页 loading：`.m-page-prizes` 绑定 `v-loading="myPrizesLoading && !myPrizeList.length"`（首次进入无数据时整页 loading；翻页时有数据不遮整页，用底部「加载中...」）
- 修复「全部刮开」报错 `Cannot read properties of null (reading 'img')`：
  - 根因：`openBatchScratch` 中 `batchPrizes = new Array(times).fill(null)`，`fetchPrize` 异步返回前的空位或 `fill(null)` 补位项为 null；`revealBatch` 渲染 `u-result` 访问 `null.img` 报错
  - `revealBatch`：`batchPrizes = batchPrizes.map(item => item || this.thanksPrize())` 空位兜底「谢谢惠顾」
  - 模板 `u-result` 判空防御：`item ? item.img : miss.jpg` / `item ? item.name : "谢谢惠顾"`
- 批量「拿下」不再叠加结果弹窗：`claimBatch` 移除 `resultPrizes` / `showResult = true`（与单卡 `claimSingle` 一致，点击拿下直接关闭连刮弹窗）
- 我的奖品滚动容器从 `.m-prize-records` 移到 `.m-record-list`：
  - 模板：`@scroll="onRecordsScroll"` 移到 `.m-record-list`；「加载中/没有更多了」提示移入 list 内末尾
  - less：`.m-prize-records` 改为 `flex` 容器（`overflow: hidden`），标题 `.m-section-title-2` `flex-shrink: 0` 固定；`.m-record-list` 承担 `flex: 1; overflow-y: auto` 滚动
  - 注：用户已把 `.m-record-item` 背景改为 `#f9f8ff`、`border: 1px solid #e8e8e8`，`.m-section-title-2` margin 微调